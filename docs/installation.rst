.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install yagocd

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv yagocd
    $ pip install yagocd
